# Memory — mind-mapper Round 4 built + gate-passed (2026-07-19)

Same-day arc: dogfood drive #3 (9 findings) → Round 3 merged to develop
(a0c6f2a, drive as human gate) → Round 4 convened, built, and cold-gate PASSED
on `feature/mind-mapper-round4` (984 tests green).

Shipped: per-node **action slots** (agent-authored CTAs on nodes and pending
proposals; ratify re-homes them onto the minted node) + **ratify-anywhere** via
a shared NodeContextMenu; **automated activity ladder** (auto-received,
received→stalled TTL, agent send as terminal act); **doc-kind honesty** (untyped
intake, `''` sentinel + `kind_author`); **build stamp + stale-dist guard**;
always-open search; selection→ground bundling (open doc = rail selection). Gate
found and rework fixed the round's one silent-loss trap: `send --ground` repeat
flags were last-wins.

Method: ratify falsified 2/7 claims (A1 proposals-home hole — both owners
independently; K1 SQLite NOT NULL immovable — measured). First-ever first-drive
gate pass; zero wire-guess failures for the second round running. New process
hazard: verification builds on the shared tree bake peers' uncommitted src into
artifacts.

Branch awaits **Cole's dogfood drive #4 + merge ruling**. Design queue for round
5: data-adjustment taxonomy, node-anchored submaps, OKF adapter.

Details:
`docs/projects/mind-mapper/sessions/2026-07-19-round4-action-slots-build.md`.
