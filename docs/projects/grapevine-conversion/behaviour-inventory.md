# Grapevine watch surface — behaviour inventory

**Created:** 2026-09-05 · **Extracted from:**
`plugins/spellbook/skills/grapevine/scripts/watch.html` at `0bceaf6` (1,000
lines; markup 515–706, script 707–998) · **Purpose:** the oracle for the React
rewrite at `src/grapevine/surface/`. One row per observable behaviour, with the
line range that implements it and how to drive it against a real daemon.
**Amended 2026-09-05 (UX branch, `feat/grapevine-ux`):** this is the surface's
living contract, not the conversion's artefact. The rows under _Channel
lifecycle from the surface_ (L1–L6) are new behaviour; R1 and the visible-states
checklist are rewritten in place and marked; the `watch.html` column is `—` for
rows the old page never had.

The **Driven** column is filled by the implementing agent (dev + release) and
re-filled by a verify agent who did not write the code. Values: `dev`,
`release`, `both`, `test` (covered by an automated cell — name it), or
`not: <why>`.

## How to drive it

```sh
# a scoped daemon, so a live ~/.grapevine daemon is never touched
export GRAPEVINE_HOME=$(mktemp -d)
CLI=plugins/spellbook/skills/grapevine/scripts/cli.ts
bun $CLI open roundtable                           # creates the channel
bun $CLI topic roundtable "design review"          # sets its topic
bun $CLI watch roundtable                          # opens http://127.0.0.1:<port>/watch#roundtable
bun $CLI send roundtable --from agent-a "hello"    # a message
bun $CLI send roundtable --from agent-b --in-reply-to 1 "hi back"   # a threaded reply
bun $CLI tail roundtable --as agent-c              # a named subscriber in the roster
bun $CLI archive roundtable                        # the archived state
bun $CLI alias cole                                # the /identity default
```

Mode: the daemon's `GET /` reports `mode`; release iff
`plugins/spellbook/skills/grapevine/dist/index.html` exists, else dev; the env
`SPELLBOOK_SURFACE_MODE=dev|release` overrides. To drive **dev** in a checkout
that has a committed `dist/`, export the override before the first verb that
spawns the daemon.

## Routes the surface calls

| ID  | Behaviour                                                                                                                                                                                                                                                                                      | `watch.html`            | Drive                                                                                                                                               | Driven                                                                                                                                                                                                                                                                                                                    |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1  | `GET /identity` on **every** init (_amended 2026-09-05, UX branch: was only when no `localStorage["grapevine:alias"]`_); `j.alias` (if truthy) pre-fills the alias **only when** no localStorage override exists — the fetch also supplies the alias a lurker's topic edit is signed with (L3) | 751–757                 | `alias cole`, clear localStorage, reload → You box shows `cole`; with an override set, reload → the fetch still happens, the box keeps the override | both — amended row driven on the UX branch: override `verifier` + `/identity` `cole` → box `verifier`, topic edit while lurking signed `cole`                                                                                                                                                                             |
| R2  | `GET /channels/<c>/tail?since=<highest>` + `&as=<alias>&human=1` (join) or `&lurk=1` (lurk); an `EventSource`                                                                                                                                                                                  | 818–824                 | Network tab on load (lurk) and after Join (as+human)                                                                                                | both                                                                                                                                                                                                                                                                                                                      |
| R3  | `GET /channels/<c>/subscribers` → `subscribers[]`, `humans[]`, `topic`                                                                                                                                                                                                                         | 868–878                 | roster changes within 3 s of a `tail --as`                                                                                                          | both                                                                                                                                                                                                                                                                                                                      |
| R4  | `GET /channels` → channel list (`name`, `subscribers`, `archived`)                                                                                                                                                                                                                             | 880–903                 | `open other` → appears within 3 s                                                                                                                   | both                                                                                                                                                                                                                                                                                                                      |
| R5  | `DELETE /channels/<name>` from the close button                                                                                                                                                                                                                                                | 905–921                 | close a channel; `list` no longer shows it                                                                                                          | release                                                                                                                                                                                                                                                                                                                   |
| R6  | `POST /channels/<c>/messages` `{from, text, in_reply_to?}` with `content-type: application/json`                                                                                                                                                                                               | 972–995                 | join, send; `tail` sees it                                                                                                                          | both                                                                                                                                                                                                                                                                                                                      |
| R7  | Channel name is `encodeURIComponent`-ed in every route above                                                                                                                                                                                                                                   | 549, 822, 871, 911, 979 | a channel with a `.` or unicode name; routes still resolve                                                                                          | not — and undrivable BY CONSTRUCTION: the daemon's name grammar (`daemon.ts:192`, `^[a-zA-Z0-9_-]([a-zA-Z0-9_.-]{0,62}[a-zA-Z0-9_-])?$`) is entirely URL-unreserved, so `encodeURIComponent` is a no-op for every name the daemon accepts (`open "odd name"` → `invalid channel name`). The unit cells are the right home |

## SSE events consumed

| ID  | Behaviour                                                                                                                                                                                            | `watch.html`                 | Drive                                                                                | Driven                                                                                                                                                                                                                                                                                                                                                |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------- | ------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| E1  | `subscribed` → if `d.topic !== undefined` set topic; status text becomes `joined <c> as <alias>` (join) or `subscribed to <c>` (lurk)                                                                | 825–835                      | load page → status bar text; topic header fills                                      | both                                                                                                                                                                                                                                                                                                                                                  |
| E2  | `message` → parse; `highest = max(highest, m.id)` (numeric ids only); push to list; index by id; `kind === "topic"` also sets the header topic                                                       | 836–850                      | `send` → row appears; `topic` verb → header updates                                  | both                                                                                                                                                                                                                                                                                                                                                  |
| E3  | Auto-scroll after append **only if** the viewport was within 80 px of the bottom before the push; reading history is never interrupted                                                               | 842–854                      | scroll up, `send` → no jump; scroll to bottom, `send` → follows                      | release — both arms (top: scrollTop stayed 0; bottom: followed). ⚠ Under a 6-message burst ~20 ms apart the feed stops following (gap 295 px); the ORIGINAL does the same (gap 300 px, measured on the 5776fa0 page): `scroll-behavior: smooth` animates each assignment and the next measurement lands mid-animation. Shared quirk, not a regression |
| E4  | `error` → `disconnected = true`; status `disconnected — reconnecting…`; close the stream; reconnect after 1 s with `since=highest` (so nothing sent during the gap is lost)                          | 857–865                      | `stop` the daemon → status turns warn; run any verb to respawn → reconnects, backlog | release (stream cut by a proxy restart)                                                                                                                                                                                                                                                                                                               |
| E5  | **Generation guard**: every handler ignores events whose stream generation is not the current one, so a superseded `EventSource` (after toggle or reconnect) can neither render nor double-reconnect | 743, 806, 826, 837, 858, 863 | toggle join/lurk rapidly → exactly one stream alive, no duplicate rows               | release (8 reconnect attempts, zero duplicate rows)                                                                                                                                                                                                                                                                                                   |
| E6  | Malformed event data is swallowed (`try/catch {}`), never rendered, never breaks the stream                                                                                                          | 827–834, 838–855             | `test`-only (no CLI path emits malformed frames)                                     | verify — driven: a proxy injector prepended a malformed `subscribed` and `message` frame to every `/tail`; both swallowed, the real frames landed, stream survived, 0 console errors                                                                                                                                                                  |
| E7  | The daemon's `: hb` comment frames are ignored (standard `EventSource` behaviour; no client code)                                                                                                    | —                            | idle 10 s → no rows, no status change                                                | both                                                                                                                                                                                                                                                                                                                                                  |

## Channel selection and the rail

| ID  | Behaviour                                                                                                                                                                                                               | `watch.html`              | Drive                                                                                  | Driven                                                                                                                                                                                                                                                                                                |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- | -------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1  | Channel = `decodeURIComponent((location.hash \|\| "#lobby").slice(1)) \|\| "lobby"` — no hash and `#` alone both mean `lobby`                                                                                           | 712–713                   | `/watch`, `/watch#`, `/watch#roundtable`                                               | release (`/watch` → lobby, `#roundtable`); the bare `#` arm is test-only                                                                                                                                                                                                                              |
| C2  | `document.title = "grapevine · <channel>"`                                                                                                                                                                              | 746                       | tab title                                                                              | both                                                                                                                                                                                                                                                                                                  |
| C3  | `hashchange` → `location.reload()` — switching channels is a full reload (stream + list reset)                                                                                                                          | 774–777                   | click another channel in the rail → page reloads on the new channel                    | release (rail click → navigation type `reload`)                                                                                                                                                                                                                                                       |
| C4  | Rail rows are links to `#<encodeURIComponent(name)>`                                                                                                                                                                    | 549                       | hover a row; click → C3                                                                | both                                                                                                                                                                                                                                                                                                  |
| C5  | Active row (name === current channel) highlighted                                                                                                                                                                       | 547, 150–152, 170–173     | current channel reads as selected                                                      | both                                                                                                                                                                                                                                                                                                  |
| C6  | A channel that first appears on a poll **after** the first one flashes (`isNew`); the first poll never flashes                                                                                                          | 884–896, 153–155, 214–222 | `open other` while the page is up → row flashes once                                   | release (caught the `animate-flash` class 52 ms after the poll)                                                                                                                                                                                                                                       |
| C7  | Archived channel: 🔒 with title `archived — read-only`, muted name                                                                                                                                                      | 552–557, 416–418          | `archive other` → lock appears within 3 s                                              | release                                                                                                                                                                                                                                                                                               |
| C8  | Subscriber count badge = `c.subscribers ?? 0`                                                                                                                                                                           | 558, 181–195              | `tail --as x` → count ticks                                                            | both                                                                                                                                                                                                                                                                                                  |
| C9  | Empty rail: italic `no channels yet`                                                                                                                                                                                    | 537–542                   | fresh `GRAPEVINE_HOME`, load `/watch` before lobby is opened — brief window; or `test` | verify — driven on an empty HOME (`pull zzz` then `close zzz`): `no channels yet` from 46 ms to 3,062 ms on first paint, then `lobby 0`. The author's reason ("shorter than one poll") was WRONG: the first `/channels` fetch precedes the tail's auto-create of lobby, and the next poll is 3 s away |
| C10 | Close button (🗑, hover-reveal, title `Close channel “<name>” (deletes message log)`): confirm → `DELETE`; forget it from the seen set; refresh; if it was the current channel, `location.hash = "lobby"` (→ C3 reload) | 561–567, 905–921          | close a non-current channel → gone; close the current one → lands on lobby             | release — both arms: non-current (author); current-channel → `#lobby` with `navigation.type === "reload"` (verify)                                                                                                                                                                                    |
| C11 | Confirm text: `Close channel "<name>"? This deletes its message log and disconnects any subscribers. This cannot be undone.`; cancel does nothing                                                                       | 906–909                   | cancel → channel remains                                                               | release (both cancel and confirm)                                                                                                                                                                                                                                                                     |
| C12 | `channelArchived` is derived from the channel list for the current channel each poll; gates the composer (P1/P2) and the reply button (F6)                                                                              | 897–900                   | `archive` the current channel → composer disappears within 3 s                         | release                                                                                                                                                                                                                                                                                               |
| C14 | An archived channel's name is muted in the rail **whether or not it is the active row** — `.row.archived .name` outranked the active colour on the span                                                                 | 416–418, 170–173          | archive the current channel → its name is muted while its row is highlighted           | release — the first surface muted only inactive rows; corrected to always (verify finding)                                                                                                                                                                                                            |
| C15 | The document title is the static `grapevine` before init sets C2's value                                                                                                                                                | 6, 746                    | first paint                                                                            | test-only: index.html `<title>`; the settled value is C2                                                                                                                                                                                                                                              |
| C13 | Rail and roster re-poll every 3 s (two independent intervals), plus once on init                                                                                                                                        | 766–773                   | timing of C6/C7/C8/S3                                                                  | both                                                                                                                                                                                                                                                                                                  |

## Header and topic

| ID  | Behaviour                                                                                                | `watch.html`   | Drive                                                 | Driven                                                                   |
| --- | -------------------------------------------------------------------------------------------------------- | -------------- | ----------------------------------------------------- | ------------------------------------------------------------------------ |
| H1  | Header: 🌿, `grapevine · <channel>`                                                                      | 516–522        | visual                                                | both                                                                     |
| H2  | Topic line: the topic, or italic placeholder `no topic set`                                              | 523–529, 85–87 | fresh channel → placeholder; `topic` verb → text      | both                                                                     |
| H3  | Topic has three sources, last write wins: `subscribed` event, subscribers poll, a `kind:"topic"` message | 829, 850, 876  | set via CLI while page is up → updates without reload | release (subscribed event + topic message; the poll source not isolated) |

## Message feed

| ID  | Behaviour                                                                                                                                                                                                          | `watch.html`      | Drive                                                       | Driven                                                                                                |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------- | ----------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| F1  | Empty feed: 🌿 + `Waiting for messages on this channel…` while `messages.length === 0`                                                                                                                             | 575–578           | fresh channel                                               | both                                                                                                  |
| F2  | Row: `from` in the alias colour, timestamp `HH:MM:SS` (locale, 2-digit), body in a card with `white-space: pre-wrap`                                                                                               | 599–614, 790–797  | `send` with a newline in the text                           | both                                                                                                  |
| F3  | `kind` styling: `message` = card; `topic` = dashed accent border, italic, and `from` reads `<from> set topic`; `announcement` = warn left border and `from` suffixed ` · announced`; `status` = no special styling | 582, 603, 261–281 | `topic` verb, `announce` verb                               | release (`topic`, `announcement`; `status` kind not driven — no fixture emitted one)                  |
| F4  | Threaded reply with a resolvable parent (`msgById(in_reply_to)`): quote line `↳ <parent.from> <snippet>` above the row                                                                                             | 585–598, 370–384  | send a reply to id 1                                        | release                                                                                               |
| F5  | Any row with `in_reply_to != null` is indented (`is-reply`) — even if the parent is not in the list                                                                                                                | 582, 367–369      | reply to an id outside the loaded backlog                   | release                                                                                               |
| F6  | Reply button (hover-reveal) on a row iff `mode === "join"` and `kind !== "topic"` and channel not archived; click → P3 and focus the composer                                                                      | 606–612, 941–944  | lurk → no button; join → button                             | both                                                                                                  |
| F7  | Alias colour is deterministic: `hsl(hash(alias) mod 360, 70%, 70%)` — same alias, same hue, in the feed, the roster, the quote and the reply banner                                                                | 781–789           | two messages from one alias share a colour                  | release (visual: same alias, same hue across rows)                                                    |
| F8  | Snippet = first 80 chars + `…` when longer                                                                                                                                                                         | 938–940           | reply to a long message                                     | both — author: `test: feed.test.ts`; verify drove it: an 81-character snippet (80 + `…`) in the quote |
| F10 | Rows are keyed by `m.id ?? idx` — a message without a numeric id still renders, keyed by its position                                                                                                              | 579               | `test` (feed.test.ts: id-less message renders, not indexed) | test                                                                                                  |
| F9  | Backlog on connect: `since=0` on first load renders the whole log; `since=highest` on reconnect renders only what was missed                                                                                       | 822, 840–841      | reload → full history; E4 → only the gap                    | release (full backlog on load; only id 30 after the gap)                                              |

## Composer (join mode only)

| ID  | Behaviour                                                                                                                                                                                      | `watch.html`     | Drive                                                             | Driven                                                                                                                                                                                                 |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| P1  | Composer visible iff `mode === "join"` and channel not archived; sticky to the bottom of the feed                                                                                              | 620, 320–326     | lurk → hidden; join → shown                                       | both                                                                                                                                                                                                   |
| P2  | Archived channel: `🔒 this channel is archived — read-only` note instead (in either mode)                                                                                                      | 617–619, 419–428 | `archive` current channel                                         | release                                                                                                                                                                                                |
| P3  | Reply banner above the box while `replyingTo`: `↳ replying to <from> <snippet>` and a ✕ (`aria-label="Cancel reply"`) that clears it                                                           | 621–641          | click reply; click ✕                                              | release (banner + ✕)                                                                                                                                                                                   |
| P4  | Textarea placeholder `message as <alias>…`; **Enter sends, Shift+Enter inserts a newline**; the form's submit also sends                                                                       | 642–655          | type, Enter → sent; Shift+Enter → newline                         | both (Enter in release; Shift+Enter newline in dev)                                                                                                                                                    |
| P5  | Send button disabled while `draft.trim()` is empty                                                                                                                                             | 656–660          | empty/whitespace draft                                            | both                                                                                                                                                                                                   |
| P6  | Send: `POST` R6 with `in_reply_to` when replying; **on `r.ok` only** clear the draft and the reply state; on failure the draft is kept; no optimistic insert — the row arrives over the stream | 972–995          | send → row arrives via SSE; `archive` then send → 409, draft kept | both (success arm); failure arm driven by verify with `page.route` — a routed 409 and an aborted fetch both kept the draft AND the banner; the daemon's own 409 on an archived channel shown with curl |
| P7  | Send is a no-op unless join mode and non-empty text                                                                                                                                            | 973–974          | covered by P1/P5                                                  | test: identity/feed cells + P1/P5                                                                                                                                                                      |

## Roster ("On the line")

| ID  | Behaviour                                                                                                                 | `watch.html`     | Drive                                                                    | Driven                                                                                                                                                                                                                                                      |
| --- | ------------------------------------------------------------------------------------------------------------------------- | ---------------- | ------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S1  | Empty: italic `no one currently subscribed`                                                                               | 670–675          | lurking alone (lurkers are never counted)                                | both                                                                                                                                                                                                                                                        |
| S2  | Entry: a dot + label in the alias colour; label is `<a> (you)` if `a === alias`, `<a> (human)` if in `humans`, else `<a>` | 676–679, 927–931 | join → `you`; `tail --human --as bob` → `(human)`; `tail --as x` → plain | release (agent-c / bob (human) / cole (you))                                                                                                                                                                                                                |
| S3  | Refreshed by the 3 s poll, on init, and immediately after a join/lurk toggle                                              | 766, 772, 969    | toggle → roster updates without waiting                                  | release — the CALL is immediate; the RESULT is not reliable in either page: the immediate `refreshSubscribers()` races the stream's registration (verify saw `no one currently subscribed` 800 ms after Join; the 3 s poll fixed it). Shared quirk, like E3 |

## Identity and lurk / join

| ID  | Behaviour                                                                                                                                                                                                                                                                                             | `watch.html`     | Drive                                                                                                                                                                          | Driven                                                                                                                                                                                                                                                    |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| I1  | Alias initial value: `localStorage["grapevine:alias"]`, else R1's `/identity`, else empty                                                                                                                                                                                                             | 736, 751–757     | R1                                                                                                                                                                             | both                                                                                                                                                                                                                                                      |
| I2  | Alias input (`placeholder="set an alias"`) is **disabled in join mode**; it is a live field that **keeps focus while typing** and commits to I3 **only on change** — blur or Enter — never per keystroke (`x-model` + `@change`); the Join toggle's enabled state follows the typed value as you type | 685–691          | click the box, TYPE `cole` with a per-key delay (not `fill()`), then check `activeElement` is still the input, the value is `cole`, and localStorage is unset until blur/Enter | release — typed with a 60 ms per-key delay after the fix (finding 1): focus kept, value `cole`, storage null until Enter, then `cole`; the first surface remounted the input per keystroke (`key={alias}` + per-key commit) and `fill()` could not see it |
| I3  | `setAlias`: trim; non-empty → `localStorage.setItem("grapevine:alias")`; empty → `removeItem` (so the next load falls back to `/identity`)                                                                                                                                                            | 946–952          | clear the field, reload → `/identity` value returns                                                                                                                            | release (override persists across a real load; cleared → `/identity` returns)                                                                                                                                                                             |
| I4  | Mode defaults to **lurk**; restored to join iff `localStorage["grapevine:mode:<channel>"] === "join"` **and** an alias resolved — per channel                                                                                                                                                         | 761–764          | join, reload → still joined; switch channel → lurk                                                                                                                             | release                                                                                                                                                                                                                                                   |
| I5  | Toggle button label: `Join channel` (lurk) / `Joined — click to lurk` (join); disabled in lurk while the alias is blank; "joined" styling when joined                                                                                                                                                 | 692–698, 490–512 | blank alias → disabled                                                                                                                                                         | both (the blank-alias disabled state seen in dev after clearing; label both)                                                                                                                                                                              |
| I6  | `toggleMode`: lurk→join requires a non-blank alias (commits it via I3); join→lurk; persists the choice for this channel; **reconnects the stream** (R2 params change) and refreshes the roster                                                                                                        | 954–970          | toggle → new tail request in Network; roster shows/hides you                                                                                                                   | both                                                                                                                                                                                                                                                      |
| I7  | In join mode the stream registers named + human presence; in lurk it registers **no** presence — lurking is invisible to `who` and to the count badge                                                                                                                                                 | 818–821          | lurk → `who` shows nobody; join → shows you                                                                                                                                    | both (`who` empty while lurking, named once joined)                                                                                                                                                                                                       |

## Status bar and connection

| ID  | Behaviour                                                                                                                        | `watch.html`               | Drive | Driven                                       |
| --- | -------------------------------------------------------------------------------------------------------------------------------- | -------------------------- | ----- | -------------------------------------------- |
| T1  | Sticky bottom bar: pulsing green dot + text; initial `connecting…` → `connecting to <c>…` on connect → E1's text on `subscribed` | 703–705, 715, 809, 293–310 | load  | both                                         |
| T2  | Disconnected: dot turns warn colour, text `disconnected — reconnecting…`                                                         | 311–313, 859–860           | E4    | both (dot `bg-attention` = rgb(240,178,101)) |
| N1  | `connect()` closes any prior stream first, bumps the generation, clears `disconnected`                                           | 803–810                    | E5    | release                                      |
| N2  | Every (re)connect passes `since=highest`                                                                                         | 822                        | F9    | release (`since=29` on every retry)          |

## Error handling (what is deliberately silent)

| ID  | Behaviour                                                                                                                                                         | `watch.html`            | Drive                                                         | Driven                                                                                                                 |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- | ------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| X1  | Every `fetch` (`/identity`, subscribers, channels, close, send) swallows its error; the only visible failure state is T2                                          | 756, 877, 902, 914, 994 | kill the daemon → polls fail silently, lists keep last values | release (rail and roster kept their last values while the proxy was down)                                              |
| X2  | A failed send (non-2xx or thrown) keeps the draft and the reply state                                                                                             | 990–994                 | P6                                                            | verify — driven with `page.route` (409 and abort): draft and reply state kept                                          |
| X3  | **Daemon side:** `GET /watch` returns `500` JSON `{error:"watch.html missing", details}` when the file is absent — replaced by dev/release resolution (see below) | daemon.ts 490–505       | `test`                                                        | test: release-serve.test.ts (forced-dev dies at the import; forced-release with no dist/ → 500 naming dist/index.html) |

## Route contract after the port (new rows, daemon side)

| ID  | Behaviour                                                                                                                                 | Drive                                                                  | Driven                               |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- | ------------------------------------ |
| D1  | Release (`dist/index.html` present): `GET /watch` serves `dist/index.html`; its hashed `./index-<hash>.{js,css}` resolve at the root      | copied tracked subtree with a `dist/`; browser renders                 | release + test                       |
| D2  | Dev: `GET /watch` is Bun's HTML bundle of `src/grapevine/surface/index.html`, styled (Tailwind via `bunfig.toml` in cwd `src/grapevine/`) | `SPELLBOOK_SURFACE_MODE=dev`; the stylesheet carries a surface utility | dev + test (dev-styled.test.ts)      |
| D3  | Forced dev with no surface source dies loud naming `src/grapevine/surface/index.html`, before writing the port/pid files                  | `test`                                                                 | test                                 |
| D4  | Nested paths and unknown bare filenames 404 from the static fall-through; existing JSON routes are untouched                              | `test`                                                                 | test                                 |
| D5  | `GET /` carries `mode`, and the boot line names it                                                                                        | `info` verb / stderr                                                   | both (`info` verb and the boot line) |

## Channel lifecycle from the surface (added 2026-09-05, UX branch)

Human parity on the daemon's channel-lifecycle routes. Every action here is a
shortcut for an act the agent already performs through the same route; no route
or message kind was added. The rail poll (C13) is the source of truth for every
state below — nothing is mutated optimistically; a mutating call is followed by
an immediate `GET /channels` (the next poll brought forward), the same path the
agent-driven rows (C7, C12) already take. Pure rules live in
`surface/state/lifecycle.ts` (`lifecycle.test.ts`); wiring in `useGrapevine.ts`.

| ID  | Behaviour                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Source                                                                            | Drive                                                                                                                                                             | Driven                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| L1  | **Context menu on every rail row** (the current row included): right-click, the keyboard's menu key, or **Shift+F10** on the focused row opens a `ContextMenu` (`role="menu"`) with _Edit topic_, _Archive_ / _Unarchive_ (whichever applies), a separator, and _Close channel…_ (destructive; the menu item, the dialog and the CLI verb `close` share the word — orchestrator's default, Cole may flip to _Delete…_). ArrowDown/ArrowUp move; **ArrowUp wraps from the top to the last item, ArrowDown stops at the bottom** (Base UI's default with the recipe's props); Enter/Space activate, Escape closes and returns focus to the row link                | `ChannelRail.tsx` (`ContextMenu*`, `openMenuOnShiftF10`)                          | right-click a row; focus a row link and press Shift+F10 / the menu key                                                                                            | both — mouse (release, dev), menu key (release; a native `contextmenu` event), Shift+F10 (release, dev; synthesised — Chrome fires no native event for it on macOS, measured 0), ArrowDown/ArrowUp (ArrowUp wraps, ArrowDown does not — measured by verify and re-measured) + Enter on _Unarchive_, _Archive_, _Close channel…_; Escape → focus back on the row                                                                   |
| L1a | _Archive_ / _Unarchive_ from the menu: `POST /channels/<name>/archive` / `…/unarchive`, **no confirmation** (reversible); the row's 🔒 + muted name follow the next poll (C7); with the filter off (L4) an archived non-current row disappears from the rail and the hidden count grows. **Focus** returns to the row's link after the act; when the act hid the row, to the _Show archived_ switch (the control that brings it back)                                                                                                                                                                                                                            | `useGrapevine.ts` `archiveChannel` / `unarchiveChannel`                           | menu → _Archive_ on a non-current row → `list` shows `archived: true`; _Unarchive_ → `false`                                                                      | both — `other` archived from the UI: `list` → `archived:true`, row hidden, label `(3 hidden)`; unarchived by keyboard: `list` → `false`; focus after a menu act measured on the row link (mouse and keyboard) and on the switch (`role=switch`) when the row hid. **Same state as the CLI:** `archive`/`unarchive` from the CLI on the same channel produced byte-identical row classes (`text-ink-dim`, 🔒 title) and rail order |
| L1b | _Close channel…_ from the menu opens the **same** `AlertDialog` as the 🗑 (C10/C11 text verbatim); Cancel / Escape → no `DELETE`; _Close channel_ → `DELETE` (C10 both arms unchanged); afterwards focus goes to the rail's `+` (the row is gone)                                                                                                                                                                                                                                                                                                                                                                                                                | `ChannelRail.tsx` (`setPending` from the item)                                    | menu → _Close channel…_ → Cancel; again → confirm                                                                                                                 | both — release: Escape kept the row and sent 0 `DELETE`s; keyboard (Shift+F10, ArrowUp wraps to _Close channel…_, Enter, Tab to _Close channel_, Enter) → one `DELETE`, row gone, `list` no longer has it, focus on `+` (re-driven after verify); dev: the current channel deleted from the menu → `#lobby`, `reload`                                                                                                             |
| L2  | **Create:** the `+` in the rail header (`aria-label="New channel"`, Tab-reachable, Enter opens) opens a `Dialog` (`role="dialog"`, title _New channel_) with a **Name** `Field` (focused on open) and a **Topic (optional)** `Field`; **Enter submits** from either field; _Create_ disabled while the name is blank; Escape / Cancel close and return focus to the `+`                                                                                                                                                                                                                                                                                          | `CreateChannelDialog.tsx`                                                         | click `+`, type a name, Enter                                                                                                                                     | both — typed (30 ms/key); initial focus on the Name input; Tab reaches Topic; Enter from Topic submits; Escape → focus on `+` (release)                                                                                                                                                                                                                                                                                           |
| L2a | Create sends `POST /channels {name, topic?, from?}` **without** `explicit` (the CLI's non-`open` verbs' shape), so an archived name answers **409 `archived`**; `from` is L3's alias when one resolves, else the daemon's `system`. On 2xx: `location.hash = name` (a reload, C3) — or, when it names the current channel, just a refresh                                                                                                                                                                                                                                                                                                                        | `useGrapevine.ts` `createChannel`; `lifecycle.createOutcome`                      | create `fresh-one` with topic → `#fresh-one`, header shows the topic, `topic fresh-one` on the CLI reads it                                                       | both — release: `POST /channels {"name":"fresh-one","topic":"made in the ui"}` → `#fresh-one`, `navigation.type === "reload"`, header `made in the ui`, feed row `system set topic`, CLI `topic fresh-one` → `made in the ui`; dev: `dev-made` → `#dev-made`                                                                                                                                                                      |
| L2b | **409 arm:** the dialog stays open, the Name field's description reads `“<name>” exists but is archived. Unarchive it instead?` and the footer's primary becomes **_Unarchive instead_**, which `POST`s `…/unarchive` then navigates to `#<name>`. Any other failure shows the daemon's `error` as a `FieldError` on the name (`data-invalid`), cleared on the next keystroke                                                                                                                                                                                                                                                                                    | `CreateChannelDialog.tsx`; `lifecycle.createArchivedText`                         | name an archived channel → the offer; `bad name` → `invalid channel name: "bad name"`                                                                             | both — release: `archived-two` → the text + `Unarchive instead` → `POST …/unarchive` → `#archived-two` (`reload`); `bad name` → `invalid channel name: "bad name"`, cleared on typing (the 409 also logs one browser network-layer console line, no `pageerror`, as E4's entries do); dev: `archived-one` → the same offer and navigation                                                                                         |
| L3  | **Topic edits inline in the header:** the topic line is a button (`aria-label="Edit topic"`, `title` hint, Tab-reachable); click or Enter turns it into an `Input` (sr-only label _Topic_, focused, draft = current topic); **Enter commits** (`PUT /channels/<c>/topic {topic, from}`), **Escape cancels**, **blur cancels**; Enter and Escape return focus to the topic line; the placeholder names the signer (`set a topic (as cole)…`). The header then follows the `kind:"topic"` message over the stream (E2/H3) — no optimistic set                                                                                                                      | `Header.tsx`; `useGrapevine.ts` `putTopic`                                        | join, click the topic, type, Enter; again + Escape; again + click elsewhere                                                                                       | both — release: Enter → `PUT … {"topic":"edited in the header","from":"verifier"}`, header + a `verifier set topic` row, CLI `topic` reads it; Escape and blur → header unchanged, 0 `PUT`s; keyboard Enter on the focused line opens the editor, and Enter/Escape return focus to it (`aria-label="Edit topic"` active after both); dev: `dev-cole` → `dev topic edit`                                                           |
| L3a | **`from` for the edit:** the joined alias; while **lurking**, the persisted default alias from `/identity` (R1); when neither resolves the line is **`aria-disabled`** with a `Tooltip` — `join the channel (or set a default alias) to edit the topic`. Also disabled (tooltip `archived — read-only`) on an archived channel — _the surface's own fence: the daemon's `PUT /topic` has no archived check and the CLI's `topic` verb discards its ensure's 409 (verify, 2026-09-06), so here the human path is **stricter** than the agent path until the daemon guard lands_. `aria-disabled`, not `disabled`, so the tooltip stays reachable by hover and Tab | `lifecycle.topicFrom` / `topicEditState`; `Header.tsx`                            | lurk with no alias → disabled + tooltip; `alias cole` on the CLI, reload, lurk → enabled, commit signs `cole`; archive the channel → disabled + the reason        | both — release: no identity → disabled, tooltip text verbatim; `alias cole` → lurking edit `PUT {"topic":"topic while lurking","from":"cole"}` while the You box still read `verifier` (the override is not the signer); archived → tooltip `archived — read-only`. `test`: `lifecycle.test.ts`                                                                                                                                   |
| L3b | The context menu's _Edit topic_ on the **current** row focuses the same header input; on **another** row it parks an intent (`localStorage["grapevine:intent"]`, keyed by channel, taken once on init) and switches to that channel — the editor opens after the reload                                                                                                                                                                                                                                                                                                                                                                                          | `useGrapevine.ts` `editTopicFor`; `lifecycle.parkIntent/takeIntent`               | menu on the current row → the input is focused with the topic as draft; menu on another row → `#<name>`, `reload`, editor open                                    | release — current row: input focused, `value = <topic>`; `other` from `fresh-one`: `#other`, `navigation.type === "reload"`, editor open and focused, the key consumed (`null`); `test`: `lifecycle.test.ts`                                                                                                                                                                                                                      |
| L2c | **A topic typed for a channel the rail already lists is set:** `POST /channels` sets a topic only where none exists, so the create follows with `PUT /channels/<name>/topic {topic, from}` signed like L3a (the CLI's `open --topic` + `topic` pair) — the dialog's `Set as <signer>.` is kept _(added 2026-09-06 after verify: the topic used to be dropped silently)_                                                                                                                                                                                                                                                                                          | `lifecycle.createFollowUpTopic`; `useGrapevine.ts` `createChannel`                | `+`, an existing channel's name, a topic, Enter → the header shows it; CLI `topic <name>` reads it                                                                | release (scripted after verify) — `other` + `set via create`: `POST /channels {name,topic,from}` then `PUT /channels/other/topic {topic,from}`, `#other`, header `set via create`, `GET …/topic` → the same; `test`: `lifecycle.test.ts`                                                                                                                                                                                          |
| L2d | **A failed _Unarchive instead_ stays in the dialog:** the daemon's `error` lands as the Name field's `FieldError`, the footer returns to _Create_, and focus moves to the Name field _(added 2026-09-06 after verify: the dialog used to close silently)_                                                                                                                                                                                                                                                                                                                                                                                                        | `CreateChannelDialog.tsx`; `useGrapevine.ts` `unarchiveAndGo` returns the outcome | `page.route` `…/unarchive` → 500 `{"error":…}`; name an archived channel; _Unarchive instead_                                                                     | release (scripted after verify) — dialog open, error `unarchive failed — marker still present`, focus on the Name input inside the dialog, footer `Cancel, Create`                                                                                                                                                                                                                                                                |
| L3c | **An archive that lands mid-edit cancels the edit:** when the poll flips the channel to archived while the editor is open, the editor closes (as Escape would) and focus returns to the now-`aria-disabled` line; `commit` re-reads the state so an Enter that races the poll sends nothing. The surface's fence — the daemon's `PUT /topic` and the CLI's `topic` verb have none _(added 2026-09-06 after verify, which landed a topic frame on a read-only channel through the open editor)_                                                                                                                                                                   | `lifecycle.shouldCancelEdit`; `Header.tsx`                                        | editor open with a draft; `POST …/archive` from inside the script; wait one poll; Enter                                                                           | release (scripted after verify) — editor gone after the poll, focus `Edit topic` (`aria-disabled="true"`), archived note shown; Enter → 0 `PUT`s, 0 new frames, header unchanged; `test`: `lifecycle.test.ts`                                                                                                                                                                                                                     |
| L4  | **Hide archived:** a `Switch` labelled _Show archived_ under the rail header, default **off**, persisted as `localStorage["grapevine:show-archived"] = "1"` (off removes the key); off hides every archived row **except the current channel**; the label appends `(N hidden)` while anything is hidden. Space/Enter on the focused switch toggles it; a real load keeps the choice                                                                                                                                                                                                                                                                              | `lifecycle.visibleChannels` / `hiddenArchivedCount`; `ChannelRail.tsx`            | two archived channels → rail shows the rest and `(2 hidden)`; switch on → all rows; reload → still on; archive the current channel with the switch off → it stays | both — release: `(2 hidden)`, on → 4 rows, storage `1`, real load (`navigate`) → on; Space → off, key removed, 2 rows; the current archived channel stayed listed with the filter off while `(1 hidden)` counted the other; dev: `(1 hidden)` → on by Space → 2 rows. `test`: `lifecycle.test.ts` pins the filter and the exception                                                                                               |
| L5  | **Archive / unarchive of the current channel from the UI** lands in the same state as the agent path (C12, P2, F6): within the poll the composer is replaced by the archived note (and back), the reply buttons go (and return), the row takes 🔒 + muted name, the topic line disables with `archived — read-only`; mode stays as it was                                                                                                                                                                                                                                                                                                                        | as C12 — the poll, not the click                                                  | join; menu → _Archive_ on the current row; then _Unarchive_; then the same two from the CLI, comparing the same DOM reads                                         | both — release, one probe read after each of the four acts: **UI archive ≡ CLI archive** (`composer:false, note:true, reply:0, lock:true, name text-ink-dim, topic aria-disabled`) and **UI unarchive ≡ CLI unarchive** (all inverted) — field-for-field identical; dev: UI archive of the current channel → composer gone, note shown                                                                                            |
| L6  | **Keyboard floor:** Tab order is _Edit topic_ (header) → `+` → the switch → per row: link, 🗑 (opacity-0 until hovered **or focused**) → the alias input → the toggle → wrap. Every new control is reachable; the old page's order (rows → alias → toggle) is preserved beneath the three new stops                                                                                                                                                                                                                                                                                                                                                              | `ChannelRail.tsx`, `Header.tsx`                                                   | Tab from the body and record `activeElement`                                                                                                                      | release — 14 Tabs: `Edit topic, New channel, <switch>, archived-one, 🗑, archived-two, 🗑, other, 🗑, roundtable, 🗑, <alias input>, <body>, Edit topic`                                                                                                                                                                                                                                                                          |

**Not driven:** L3's `from` when both the override and `/identity` are blank
_while joined_ — unreachable (join requires an alias, I6). The 3 s race between
an agent's `archive` and a human's in-flight topic edit (the `PUT` lands on an
archived channel: the daemon's `PUT /topic` has **no** archived check, only the
CLI's `topic` verb does not refuse either — it discards its ensure's 409; verify
2026-09-06) — a backend finding, reported in the UX journal, not worked around.

## Visible states — a checklist for the verify agent

- empty rail (C9) · empty feed (F1) · empty roster (S1)
- topic placeholder (H2) vs topic (H3)
- lurk (P1 hidden, I2 enabled, I5 `Join channel`) vs join (composer, `(you)`, I2
  disabled)
- archived channel (C7 in the rail — with _Show archived_ on, P2 in the feed, F6
  absent) · _amended 2026-09-05 (UX branch): archived rows are hidden by default
  (L4); the current channel is the exception_
- context menu open on a row (L1) · the create dialog: empty, error, 409 (L2) ·
  the topic editor open (L3) · the topic line disabled with its tooltip (L3a) ·
  the rail with `(N hidden)` and with the switch on (L4)
- reply banner (P3) and a threaded row (F4, F5)
- new-channel flash (C6)
- connected (T1) vs disconnected (T2) vs reconnected with the gap replayed (E4,
  F9)
- topic / announcement / status message kinds (F3)
