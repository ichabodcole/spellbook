# Project Status Sweep

**Report Date:** 2026-08-10 · **Report Type:** Doc Status · **Generated By:**
`review-docs` orchestration — one `docs-curator` agent per project (10 agents),
each validating the docs against `plugins/spellbook/skills/*`, git history, and
live tests.

## Executive Summary

Reviewed all 10 active folders under `docs/projects/`. **Four are complete and
archivable**, **one is a misfiled backlog item**, and **five carry real
remaining work**. Separately, **six open GitHub issues were fixed and shipped in
v2.2.0 and never closed**.

Acting on both cuts the visible surface roughly in half:

|                 | before | after |
| --------------- | ------ | ----- |
| active projects | 10     | 5     |
| open issues     | 13     | 7     |

**The health finding is not the backlog size — it is that the backlog could not
be sized from the documents.** Four projects' status lines disagreed with the
tree, in both directions (see _The cross-cutting finding_). The reason this
sweep needed ten agents reading code rather than one agent reading headers is
itself the result.

## Scope

- **Document types:** every folder under `docs/projects/` except `_archive`,
  `TEMPLATES`, `README.md`.
- **Method:** one `docs-curator` per project, instructed to **verify against
  code and git history and to distrust the status lines**. The `spell-hardening`
  agent was additionally given a conclusion already reached by the orchestrator
  and told to falsify it. **It did.** The orchestrator had called enforcement
  "barely started"; three roster-wide conformance suites already ship. The
  verdict survived, the characterisation did not — corrected in place under
  `spell-hardening` below.
- **Each verdict below cites paths, shas, or test names.** Claims the agents
  could not ground are marked as such rather than smoothed over.

## Findings by Status

### Archivable (4)

| Project                       | Verdict    | Evidence                                                                                                                                            |
| ----------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| `cross-project-observatory`   | ARCHIVE    | astrolabe shipped `5dfa9a8`; `scripts/{server,cli,state}.ts` + 827 lines of tests; `trigger-registry.md` marks it `shipped`                         |
| `imago`                       | ARCHIVE    | three plans at 100% (context-library 10 tasks, layer-system 4 phases, refs-as-assets 3 phases); 111 tests; last change `5e6aacd` (2026-08-08)       |
| `magpie-rebuild`              | ARCHIVE    | all seven phases shipped (intake → phase scaffold → slice → remove → retry → expand → export); 76 tests; live at `plugins/spellbook/skills/magpie/` |
| `spell-architecture-maturity` | SUPERSEDED | canonical daemon+thin-CLI rule written `7719c34` (`house-style.md:188–225`); bounty migrated `d097474`; `bounty-agent-usable` archived `ef29f10`    |

Deferred work in the archived projects is **explicitly deferred with no branches
and no tracking** — imago's masking / capture-style retune / per-project
scoping, magpie's three slice-UI paper cuts and multi-image. Archiving does not
lose it; the archive note carries it.

### Misfiled (1)

- **`digestify-image-viewer`** — a single 58-line `backlog.md` whose own header
  reads `**Status:** idea / backlog (not scheduled)`. No proposal, no plan, no
  sessions, nothing built (`digestify/scripts/review.ts` is still text-only).
  Its deferral condition — _"revisit after glamour coalesces"_ — was satisfied
  on 2026-06-25 (`22f22ab`) and nobody revisited. **Belongs in
  `docs/backlog/`.**

### Real Remaining Work (5)

| Project                  | Verdict    | The gate on archiving it                                          |
| ------------------------ | ---------- | ----------------------------------------------------------------- |
| `spell-hardening`        | NEEDS-WORK | end condition part 2 — sprint 05, "the gate"                      |
| `mind-mapper`            | NEEDS-WORK | R13 built + gated, **awaiting testing**, then a scope call on R14 |
| `spellbook-coherence`    | NEEDS-WORK | 1 of 4 deliverables complete                                      |
| `spell-surface-pipeline` | NEEDS-WORK | hypothesis **untested**; none of the mechanism exists             |
| `spellbook-rebrand`      | NEEDS-WORK | visual front blocked on an aesthetic decision                     |

#### `spell-hardening` — not archivable, and its own roadmap says why

`roadmap.md:120–127` defines a **two-part** end condition:

> **1. The population is drained** — no open issue or backlog item in the
> absence / parity / consistency family that blocks another team or us. **2. The
> rules exist and are enforced** — `grimoire/house-style.md` carries them in its
> mandatory shape, and a conformance gate **fails** a new violation.

**Part 1 is met** once the six shipped-but-open issues are closed. **Part 2 is
partially met.** Three roster-wide conformance suites already ship and run:

```
grimoire/flag-invariant.test.ts        SKILL.md ↔ entry points, 2-way
grimoire/rule-id.test.ts               every rule carries a stable id
grimoire/exit-site-inventory.test.ts   roster-wide inventory
```

What is missing is narrower than "enforcement": there is **no outcome-envelope
gate** (nothing asserts that every spell's responses carry `outcome:` on the
right paths, or catches absent-vs-null confusion), **no bidirectional rule↔check
link** (clause (ii): every rule names its check, every check cites its rule, and
_the link itself is gated_), and **no gating of the prose rules at all** —
`bun run check` is `biome check` only.

That set is exactly sprint 05, "the gate", already forecast in the roadmap in
two halves: **(a) the harness** (share `flag-invariant`'s behavioural
enumerator, mechanise three behavioural rules, mutation-calibrate every cell)
and **(b) the restatement** (turn two intent-bearing rules — `null` not `0`;
no-op not failure — into structural predicates), which gets its own ratify
round.

#### `spellbook-coherence` — the blocker was never real

The status line reads _"Deliverable 3 typecheck gate blocks completion."_
Nothing was ever blocked. `typescript` sits in `peerDependencies`
(`package.json:37`) and never in devDeps; `tsc --noEmit` appears nowhere; the
`check` script has been `biome check --error-on-warnings .` since init. The gate
was not blocked — **it was never started**, and the status line has been
describing a wall that does not exist. `tsconfig.json` is already
`strict: true`, so wiring it is two lines plus whatever latent errors the first
run surfaces.

Deliverables: 1 of 4 complete (magpie's thin Bun wrapper over Python). Feedback
touchpoints are 3 of 4 — **bounty's `SKILL.md` has none**, the only spell
missing one, which `ward`'s own checklist requires. Four
`grimoire/decay-ledger.md` rows (47, 49, 56, 57) are still pure `(seed)` with no
spell-based validation.

#### `spell-surface-pipeline` — the plan reports work that was not done

The hypothesis is **untested**, and three checks establish it:

| claim                        | reality                                                                               |
| ---------------------------- | ------------------------------------------------------------------------------------- |
| astrolabe builds to `dist/`  | no `dist/` — the skill has `surface/` only                                            |
| dev-only dynamic import      | `astrolabe/scripts/server.ts:63` — static `import index from "../surface/index.html"` |
| canon written to house-style | `grimoire/house-style.md:361` still reads `## The build (there isn't one)`            |

v2.2.0 shipped astrolabe as surface **source**, which is precisely the state the
success criterion said a validating release would replace.

**The defect here is the document, not the gap.** `plan.md` is headed
`RATIFIED — all seams settled`, and Seam A states that circe _"empirically
built"_ astrolabe's surface. That is design intent written in the past tense,
and it reads as a report of completed work. The astrolabe React re-home that
_did_ happen (`5dfa9a8`) was the **prerequisite supply** for the pilot, not the
pilot.

#### `spellbook-rebrand` — known fixes exist but are blocked upstream of themselves

Naming front closed (`51dd7b5`; tuskboard retired in the registry; marketplace
tag correct). Every stale reference left is in bounty and is the **mammoth**,
not the name: `bounty/SKILL.md:23–24`, `bounty/scripts/template.html:35`
(`/* mammoth gold */`), `bounty/assets/README.md:38,42–44`, and the binaries
`assets/{wordmark,mascot,mascot-large}.webp` + `favicon.png`, all untouched
since the rename.

Five mechanical `[known-fix]` items sit behind three `[needs-design]` decisions
(reuse mascots restyled vs. retheme; the shared palette / type / sigil; codify
the cute-occult direction). **Regenerating assets before the aesthetic is
settled means doing it twice** — this is not a quick-win project despite looking
like one.

#### `mind-mapper` — finished work outside the tree, deliberately

`feature/mind-mapper-round13` is **9 commits ahead of develop**: code (`569c4e5`
edge-verb family, `9cf8063` event coverage + card legibility), stamped dist
(`94bf363`), docs + Contract 13 (`5ee671c`), cold drive with fixes folded in
(`ed15bf5`), and current with develop as of today. **It is held pending
testing** — a deliberate gate, not a stall.

The twelve `plan-round*.md` files are **not sprawl**: they are linear
supersessions (R3→R12), each driven by the correspondingly numbered
`drive*-findings.md`. Only the latest describes current state. After R13 lands,
the round trail can be consolidated into `_archive/`.

Remaining: **F3 status channel** (render-only; the wire shipped in R11 — a small
round) and **F1 frames + pin nodes** (`[needs-design]`, needs a dagre-vs-ELK
compound-node decision). Images and advanced multi-agent routing are explicitly
deferred.

## Six Issues Fixed, Shipped, and Left Open

Closed as part of this sweep. Each was verified by locating the live test cell,
not by matching a commit subject:

| issue | what shipped                                 | proof                                                         |
| ----- | -------------------------------------------- | ------------------------------------------------------------- |
| #79   | bounty list names the noun it counted        | `unreadable` — `bounty/scripts/server.test.ts`                |
| #85   | astrolabe benign no-op names the state found | `already-raised` — `astrolabe/scripts/state.test.ts`          |
| #86   | grapevine `version_ok` can say _unknown_     | `version_unchecked_reason` — `grapevine/scripts/cli.test.ts`  |
| #87   | imago `context.add` returns the minted id    | `already-recorded` — `imago/tests/server.integration.test.ts` |
| #88   | digestify reports what was observed          | `read-then-left` — `digestify/scripts/review.test.ts`         |
| #97   | bounty keyed respawn restores by default     | `restoreFailed` — `bounty/scripts/server.test.ts`             |

## The Cross-Cutting Finding

**The documents and the tree disagreed about what exists — in both directions —
in four of ten projects.**

| direction                      | instance                                                     |
| ------------------------------ | ------------------------------------------------------------ |
| done, but reads as open        | six issues fixed and shipped, never closed                   |
| done, but reads as nothing     | `magpie-rebuild` complete with no status line at all         |
| not done, but reads as blocked | `spellbook-coherence` — a blocker that never existed         |
| not done, but reads as done    | `spell-surface-pipeline` — `RATIFIED`, _"empirically built"_ |

This is [`the unclosed unit`](../backlog/2026-08-10-the-unclosed-unit.md) at
project scale. There, the finding was that _documenting an omission is not
producing the artifact_. Here it generalises: **a status line is a claim about
the tree that nothing checks, and it decays silently in whichever direction is
least convenient.**

### This report is itself an instance, and that is the load-bearing part

`docs/reports/2026-06-27-doc-status-report.md` already named **two of the four
known fixes below** — the typecheck gate and bounty's missing feedback
touchpoint — under the heading _"the remaining implementation work … is still
open."_ That was **44 days ago**. Both are still open today. They were found,
written down, correctly categorised, and not done.

So the honest reading is that **another report changes nothing on its own.**
That is why the four known fixes are being executed on a branch alongside this
document rather than filed by it. A report that only re-files what the last
report filed is the failure it is describing.

## Recommendations

**Do now (mechanical, no design required):**

1. Close the six issues above. _(done in this pass)_
2. Archive the four complete projects; move `digestify-image-viewer` to
   `docs/backlog/`. _(done in this pass)_
3. Land the four known fixes. _(branch alongside this report)_

**Do next (still well-defined):**

4. **#98** — tail retries forever on an unresolvable pinned session (carded
   `s5-6`).
5. **#76** — heartbeat overrun false-fires on session-length cards.
6. **#11** — regenerate `bounty/assets/wordmark.webp`, which still renders
   **"Tuskboard"**. Independent of the aesthetic decision: the issue scopes it
   to the _current_ surface palette, so it fixes a wrong name rather than
   restyling. It is an image-generation task (imago), not a find-replace.
7. **F3 status channel** in mind-mapper — render-only, the wire already exists.

**Design first, then build:**

8. **Sprint 05, "the gate"** — the only thing between `spell-hardening` and its
   own end condition.
9. **The rebrand aesthetic decision** — unblocks five mechanical items.
10. **The pipeline's five slices** — and fix `plan.md`'s past-tense claim first,
    so the next reader is not misled the way this sweep was.
11. **F1 frames + pin nodes** (dagre vs ELK), **#82** cross-tool spelling
    (blocked on the noun set), **#75** bounded tail.

**Not a defect:** **#72** (task-size affordance on the board UI) is a feature
request.

**#64** (idle-death) has no reproduction — the pre-registered repro against
2.1.0 did not fire. There is nothing to fix until it does, and that is a
statement about evidence, not about priority.

## The Four Known Fixes

| #   | fix                                          | why it is well-defined                                                                                                                                                                                                                     |
| --- | -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | bounty's board never renders `restoreFailed` | `server.ts` emits it at 5 sites; `template.html` matches **0**. The agent is told the restore failed; the human sees a silently empty board and cannot distinguish it from an empty one. Exactly the lockstep-mirror drift no test guards. |
| 2   | bounty `SKILL.md` has no feedback touchpoint | `ward` requires one on every spell; bounty is the only miss. Copy digestify's structure.                                                                                                                                                   |
| 3   | typecheck gate unwired                       | `typescript` → devDeps, `check` → `tsc --noEmit && biome check`. Already `strict: true`.                                                                                                                                                   |
| 4   | stale project status lines                   | Four projects; the sweep produced the correct values.                                                                                                                                                                                      |

Fix 1 is a **consumer-visible defect** and the only one of the four that a user
feels.

## What Could Not Be Determined

Recorded rather than smoothed over, because each is a real limit on this
report's authority:

- **Whether R13 is the V1 release candidate or whether R14 is queued.** That is
  a scope call, not a tree fact.
- **Whether the deliberate non-writing of `scaffold/`** ("on purpose", per
  `scaffold/README.md`) is still house intent.
- **Whether the marketplace supports a packaging-exclude mechanism** for surface
  source (`.claudeignore` or equivalent) — Seam B asks this and it was never
  answered. The pipeline cannot be validated without it.
- **Whether `full-state broadcast vs granular diffs`** was ever decided. No
  decision is recorded either way. **Resolved after the agents reported:** the
  reviewing agent could not read `wordmark.webp` and flagged it as undetermined.
  Issue #11 answers it — the wordmark **does** literally render "Tuskboard", and
  the fix is a single asset regenerated against the _current_ surface palette.
  So it is **not** blocked behind the aesthetic decision; see the note under #11
  above.

The slip is worth keeping: the `bounty-agent-usable` branding check was
`grep -rni tuskboard`, which returned clean because **grep cannot see into a
binary `.webp`**. A green check that structurally could not observe the thing it
claimed to verify — the same shape as clause (i) of the end condition, _a gate
must state what it cannot see_. </content>
